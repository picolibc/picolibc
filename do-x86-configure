#!/bin/sh
DIR=`dirname $0`
ARCH=i386-linux-gnu
meson $DIR \
        -Dmultilib=false \
	-Dincludedir=picolibc/$ARCH/include \
	-Dlibdir=picolibc/$ARCH/lib \
	-Dtests=true \
	--cross-file $DIR/cross-$ARCH.txt \
	--buildtype debug \
	"$@"
